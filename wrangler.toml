name = "gileara"
compatibility_date = "2024-04-03"
pages_build_output_dir = ".next"

[vars]
# Add your environment variables here for local development if needed
# Better to manage them in the Cloudflare Dashboard for production

[env.production.vars]
# Production secrets should be set in the Cloudflare Dashboard
